<?xml version="1.0" encoding="UTF-8"?>
<schemalist gettext-domain="nonlinear-animation">
  <schema id="org.gnome.shell.extensions.nonlinear-animation" path="/org/gnome/shell/extensions/nonlinear-animation/">
    <key name="mode" type="s">
      <choices>
        <choice value="ease-out-cubic"/>
        <choice value="ease-out-expo"/>
        <choice value="ease-out-quart"/>
        <choice value="ease-out-quint"/>
        <choice value="ease-in-out-cubic"/>
        <choice value="ease-in-out-quart"/>
        <choice value="ease-in-out-quint"/>
        <choice value="ease-in-out-expo"/>
        <choice value="ease-out-back"/>
        <choice value="ease-out-elastic"/>
      </choices>
      <default>"ease-in-out-cubic"</default>
      <summary>Easing curve (legacy)</summary>
      <description>Legacy curve key from v1–v5. Still read once for migration to selected-curve; otherwise unused.</description>
    </key>
    <key name="schema-version" type="i">
      <default>1</default>
      <summary>Settings schema version</summary>
      <description>Bumped automatically after the one-time migration from the legacy mode key.</description>
    </key>
    <key name="selected-curve" type="s">
      <default>"preset:ease-in-out-cubic"</default>
      <summary>Selected curve</summary>
      <description>Id of the active curve: a built-in preset (preset:…) or a user curve (user:…).</description>
    </key>
    <key name="user-curves" type="as">
      <default>[]</default>
      <summary>User curves</summary>
      <description>User-created and imported curves, one JSON object per entry.</description>
    </key>
    <key name="continuity" type="b">
      <default>true</default>
      <summary>Interruption continuity</summary>
      <description>When an animation is interrupted (e.g. flipping two app-grid pages quickly), carry the current velocity into the new animation instead of re-accelerating from standstill, using a velocity-matched spring.</description>
    </key>
    <key name="spring-solver" type="s">
      <choices>
        <choice value="analytic"/>
        <choice value="numeric"/>
      </choices>
      <default>"analytic"</default>
      <summary>Spring solver</summary>
      <description>How spring curves are evaluated. analytic (default): closed-form solution, a few math calls per frame. numeric: Ultra mode, per-frame physics integration for maximum fidelity at a slightly higher CPU cost.</description>
    </key>
    <key name="threshold-ms" type="i">
      <default>100</default>
      <range min="0" max="2000"/>
      <summary>Threshold (ms)</summary>
      <description>Only animations at or above this duration get the easing curve. Lower = more things eased (hovers ~100ms); higher = fewer.</description>
    </key>
    <key name="duration-scale" type="d">
      <default>1.8</default>
      <range min="0.5" max="5.0"/>
      <summary>Duration scale</summary>
      <description>Multiplier for the duration of eased animations. 1.0 = GNOME's default (quick); 1.8 ≈ macOS; 2.5 = slow/luxurious.</description>
    </key>
    <key name="gesture-duration-scale" type="d">
      <default>1.3</default>
      <range min="1.0" max="3.0"/>
      <summary>Gesture duration scale</summary>
      <description>Duration multiplier for animations during/after a touchpad gesture (workspace switch, app-grid paging). The easing curve is kept at GNOME's velocity-matched EASE_OUT_CUBIC so there's no jolt on release; this only stretches the duration so the deceleration reads more. 1.0 = gestures fully native.</description>
    </key>
    <key name="gesture-grace-ms" type="i">
      <default>800</default>
      <range min="0" max="3000"/>
      <summary>Touchpad gesture grace (ms)</summary>
      <description>Window after a touchpad gesture (3-finger swipe/pinch) during which animations play at GNOME's native speed. Touchpad-driven overview/workspace opens already track your finger, so their wrap-up shouldn't be slowed. Set 0 to disable the exception (ease everything, including gestures).</description>
    </key>
    <key name="enabled" type="b">
      <default>true</default>
      <summary>Enabled</summary>
      <description>Master switch (A/B without disabling the whole extension).</description>
    </key>
    <key name="magic-lamp-easing" type="b">
      <default>true</default>
      <summary>Ease magic-lamp minimize/unminimize</summary>
      <description>If compiz-alike-magic-lamp-effect is installed, curve its minimize/unminimize timeline with EASE_OUT_CUBIC so the window decelerates into the dock instead of stopping dead. No-op if magic-lamp isn't installed.</description>
    </key>
  </schema>
</schemalist>
